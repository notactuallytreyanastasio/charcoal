# Deciduous database (local)
.deciduous/

# Rust build artifacts
/target/

# Environment secrets (but NOT .env.example — that's the template)
/.env

# SQLite database files
*.db
*.db-wal
*.db-shm

# Charcoal export files
charcoal-export.json

# Web viewer
web/node_modules/
web/dist/

# Chainlink local state (rules are tracked, but DB and cache are not)
.chainlink/issues.db
.chainlink/.cache/
